export const environment = {
  production: true,
  // Replaced by GitHub Actions with the actual Railway backend URL (RAILWAY_BACKEND_URL secret)
  apiUrl: 'RAILWAY_BACKEND_URL_PLACEHOLDER/api/v1'
};
